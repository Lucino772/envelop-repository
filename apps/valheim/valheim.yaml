name: valheim
config: !content apps/valheim/envelop.yaml
depots:
  - name: steam/app/896660
    config:
      os: ["*"]
      arch: ["*"]
      tags: []
    exports:
      exe: "{{ .Path }}/valheim_server{{ if eq .TargetOs \"windows\" }}.exe{{ end }}"
      save: "{{ .Path }}/envelop_world"
    manifest:
      type: steam
      appid: 896660
